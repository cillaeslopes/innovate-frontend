import React from 'react'
import Layout from '../components/Layout'

const pageTitle = 'Innovate: Posts'
const pageDescription = 'Publicações da Innovate, instagram, lives e mais'
const title = 'Posts'

const Posts: React.FunctionComponent = () => (
    <Layout
        pageTitle={pageTitle}
        pageDescription={pageDescription}
        title={title}
    />
)

export default Posts
